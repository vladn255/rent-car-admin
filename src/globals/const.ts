const AuthCredentials = {
    name: "intern",
    password: "intern-S!"
}

const RoutePath = {
    HOME: "/",
    MAIN: "/admin"
}

const SideBarItemsNames = {
    CAR_CARD: "carCard",
    CARS_LIST: "carsList",
    ORDERS: "orders",
    CATEGORIES: "categories"
}

const TIME_FORMAT = 'DD.MM.YYYY HH:mm'

export {
    AuthCredentials,
    RoutePath,
    SideBarItemsNames,
    TIME_FORMAT
}