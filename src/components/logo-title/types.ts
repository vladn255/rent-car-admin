interface ITextStyles {
    fontSize: number,
    lineHeight: number
}

interface IIconStyles {
    gap: number,
    iconHeight: number,
    iconWidth: number
}

export type { ITextStyles, IIconStyles }